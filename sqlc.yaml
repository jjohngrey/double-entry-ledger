version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/queries.sql"
    schema: "migrations/"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_json_tags: true
        emit_prepared_queries: false
